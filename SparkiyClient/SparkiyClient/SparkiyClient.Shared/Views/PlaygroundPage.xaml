<Page
    x:Class="SparkiyClient.Views.PlaygroundPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SparkiyClient.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
	RequestedTheme="Light">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		
		<RichEditBox x:Name="RichEditBox" IsTextPredictionEnabled="False" IsSpellCheckEnabled="True" Margin="4" Grid.Column="0" />
		<SwapChainPanel x:Name="SwapChainPanel" Margin="4 0 0 0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Column="1" />
    </Grid>
</Page>
